# Webhooks

Webhook resources are used by RedSeed to issue callbacks when 

Some resources are shared to many clients, others belong to a single client only. When you make a RedSeed API request you are limited to resources available to your client only.

## Client Attributes

Attribute | Type | Description
--------- | ------- | -----------
`id` | uuid | A version 4 universally unique identifier generated by RedSeed when a client is created | true
`status` | 'Active', 'Inactive' | Client status
`name` | text | Client name
`dateStarted` | datetime | The datetime the client started using RedSeed. Set manually by RedSeed staff after launch.
`dateEngagement` | datetime | The datetime that training engagement data starts for the client
`dateCreatedAt` | datetime | The datetime the client resource was created at
`dateUpdatedAt` | datetime | The datetime the client resource was last updated


## Get A Single Client

```shell
curl "<%= data.redseed.endpoint %>/clients/{UUID}" \
  -X GET \
  -H "Authorization: bearer {MY_API_TOKEN}"
```
```php
$curl = curl_init("<%= data.redseed.endpoint %>/clients/{UUID}");
curl_setopt($curl, CURLOPT_HTTPHEADER, [
  'Authorization: bearer {MY_API_TOKEN}'
]);
$response = curl_exec($curl);
```
```javascript
const redseedApi = require('redseedApi');
let api = redseedApi.authorize('{MY_API_TOKEN}');
let response = api.kittens.get('<%= data.redseed.endpoint %>/clients/{UUID}');
```

> The above command returns JSON structured like this:

```json
{
  "data": {
    "id": "",
    "status": "Active",
    "name": "Example Client",
    "product": "RedSeed Pro",
    "dateStarted": "2023-02-14T11:00:00.000000Z",
    "dateEngagement": null,
    "dateCreatedAt": "2023-02-14T19:52:57.000000Z",
    "dateUpdatedAt": "2023-02-14T19:52:57.000000Z"
  }
}
```
### URL endpoint


``
<%= data.redseed.endpoint %>/clients/{UUID}
``
### URL Parameters

Parameter | Description
--------- | -----------
{UUID} | The UUID of the client resource

### URL Query Parameters

None

<aside class="notice">
You must replace <code>{UUID}</code> with the uuid of the client resource and <code>{MY_API_TOKEN}</code> with your personal API key.
</aside>

## Update Client

Not implemented via API, please contact RedSeed Support.

## Delete Client

Not implemented via API, please contact RedSeed Support.

## Restore Client

Not implemented via API, please contact RedSeed Support.

## Get Multiple Clients

Please contact RedSeed Support if you need to access multiple client resources.
